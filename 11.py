# Calculate circle circumference
import math

radius = float(input("Enter the circle radius: "))
circumference = 2 * math.pi * radius
print(f"The circumference of circle : ",round(circumference, 2),"cm")